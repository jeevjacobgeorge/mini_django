#include <stdio.h>
int fibo(int num);
int main()
{
	int l;
	scanf("%d",&l);
	for (int i=1;i<=l;i++)
		printf("%d\n",fibo(i));
	
}
int fibo (int num)
{
	if (num ==1||num ==2)
	 return 1;
	else
		return (fibo(num-1)+fibo(num-2));
		
}
